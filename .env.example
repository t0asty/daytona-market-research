# Copy to `.env` (gitignored) — never commit real keys.
#
# Local-only (recommended): report-gen --llm
OPENAI_API_KEY="sk-your-key-here"
# OPENAI_MODEL="gpt-4o-mini"
#
# Avoid putting OPENAI_API_KEY in Daytona sandboxes if OpenAI flags datacenter use;
# use --mode playwright in sandboxes and LLM polish locally (see README).
